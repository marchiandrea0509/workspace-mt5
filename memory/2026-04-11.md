# 2026-04-11

- In Discord `#mt5`, investigated a laptop hard shutdown that interrupted the MT5/OpenClaw session.
- Default MT5 automation target remained the portable instance (`C:\MT5_OANDA_PAPER_OC\MQL5` via `mql5/MQL5`), but immediately after the shutdown that portable terminal was not running, while the old AppData-backed OANDA terminal had auto-restarted and re-synced to the broker.
- Account/broker state survived the shutdown cleanly: OANDA paper account re-authorized successfully and still showed `0 positions` / `28 orders`.
- `scripts\reload_mt5_bridge.ps1 -InstanceName oanda_paper_oc` successfully restarted the portable MT5 terminal, but the restart verification failed because `GrayPaperBridgeEA` did not auto-load on a chart after restart. This confirmed a specific shutdown weak point: the portable terminal can come back logged in without the bridge EA attached/initialized.
- Recovery required a manual reattach of `GrayPaperBridgeEA` in the portable MT5 window plus Algo Trading enabled. After that, health check confirmed both the EA load line and the init line (`Watching gray_bridge\inbox`).
- Cleanup after recovery: closed the old AppData-backed fallback MT5 process and also closed an older duplicate portable MT5 process, leaving one healthy portable MT5 process running as the sole bridge target.
- Added `docs/MT5_HARD_SHUTDOWN_RECOVERY_PLAN.md` with the observed failure mode, the operator recovery sequence, and a staged plan for future automated recovery tasks after hard shutdowns.
- Implemented the first-pass orchestrator `scripts\recover_mt5_after_shutdown.ps1`. It now verifies the pointer/target, inventories MT5 terminals, health-checks the portable bridge, restarts it if missing, safely cleans up fallback/duplicate terminals only after portable health is confirmed, and emits a structured JSON outcome summary.
- Implemented MT5 profile/persistence hardening tooling:
  - `scripts\save_mt5_recovery_baseline.ps1`
  - `scripts\restore_mt5_recovery_baseline.ps1`
  - baseline-aware recovery path via `scripts\recover_mt5_after_shutdown.ps1 -TryRestoreBaselineOnMissingEa`
- Wrote `docs/MT5_PROFILE_PERSISTENCE_HARDENING.md` documenting the key finding that `MQL5\Profiles` looks like the authoritative portable profile store here, while the top-level `Profiles\...` tree looks stale and should not be treated as the main recovery source.
- On Saturday 2026-04-11, completed a controlled portable-MT5 persistence validation window. A first restart reproduced the old failure mode (MT5 returned but EA was not auto-restored), but after reattaching the EA and capturing a fresh baseline, a later controlled restart validated the hardened path end-to-end:
  - baseline snapshot created successfully under `state\mt5_recovery_baselines\oanda_paper_oc\20260411_094722`
  - MT5 launched with the correct args: `/portable /profile:Default`
  - with fresh-log validation enabled, restart health was proven by new post-restart lines for load, authorization, synchronization, and EA init (`09:54:41` load, `09:54:42` auth, `09:54:43` sync, `09:54:43` init)
- Important repo hygiene decision: keep the raw baseline snapshot directory as local machine state, but document the validated result and the commands/logic in tracked docs/scripts.
- Added and registered a Windows Scheduled Task named `MT5 Boot Recovery` that runs at user logon for `anmar`, waits 90 seconds, then calls `scripts\invoke_mt5_boot_recovery.ps1` to run baseline-aware MT5 recovery automatically with `-SkipCompile`.
- Manually test-fired the scheduled task and confirmed it completed successfully with `RECOVERED_FULLY`; task logs are written under `state\mt5_boot_recovery_logs\`.
- Expectation caveat documented: the current automation is automatic after Windows **logon**, not yet a proven pre-login/service-style boot recovery chain.
- Key MT5 hardening commits from this session:
  - `69d835b` — document hard-shutdown recovery plan
  - `9a3dad4` — add MT5 hard-shutdown recovery orchestrator
  - `4f98f88` — add MT5 profile persistence hardening tools
  - `7483b43` — harden MT5 restart checks with fresh log validation
  - `772199b` — document validated MT5 restart hardening result
- On 2026-04-12, added stale pending-order cleanup to `scripts\mt5_open_compact_report.py` with config under `compactReportCleanup` in `config\mt5_fx_autotrade_phase1.json`. Logic: before report execution, strategy-managed pending orders are evaluated against their submission snapshot; if the current gap-to-entry is >= 75% farther than at submission and the order is at least 60 minutes old, the report path will cancel them. The compact report now also shows `dfrac` (delta vs submission frac) and `gap_delta` per pending order. Dry-run on the live book flagged only two very stale EURCAD pending orders for cancellation, which looked like a reasonable first threshold.
